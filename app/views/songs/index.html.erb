<h1> Index of Songs </h1>

<ul>
    <%=@songs.each do |s|%>
        <li> <%=link_to "#{s.artist_name} - #{s.title}", song_path(s) %></li>
        <li> <%=link_to s.artist_name, artist_path(s.artist)%>
    <% end %>
</ul>
